import Foundation

/// App secrets — copy this file to Secrets.swift and fill in real values.
/// Secrets.swift is gitignored and will not be committed.
enum AppSecrets {
    static let appToken = "YOUR_APP_TOKEN_HERE"
    static let telemetryDeckAppID = "YOUR_TELEMETRYDECK_APP_ID_HERE"
}
